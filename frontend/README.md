# Kanban Frontend

A React + TypeScript frontend using Tailwind CSS and Supabase.

## Setup

1. `cd frontend`
2. Copy `.env.example` to `.env`
3. Install dependencies:
   ```bash
   npm install
   ```
4. Run:
   ```bash
   npm run dev
   ```
